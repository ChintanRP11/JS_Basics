// of with for loop used to iterate through array or any object with multiple elements.
let hobbies = ["games", "music", "reading", "cooking", "singing", "dancing", "fishing"];
for (let hobby of hobbies) {
    console.log(hobby);
}